# ServerSee 配置文件

# API 服务器端口
api-port: 8080

# 是否启用详细日志
debug: false

# 性能数据采集间隔 (秒)
collection-interval: 60

# 是否在 API 中输出插件列表
show-plugins: false

# 重启服务器时执行的命令 (默认为 spigot 的 restart 命令)
restart-command: "restart"
